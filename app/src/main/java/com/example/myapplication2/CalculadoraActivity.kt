package com.example.myapplication2

import android.annotation.SuppressLint
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.Button
import android.widget.EditText
import android.widget.Toast

class CalculadoraActivity : AppCompatActivity() {
    @SuppressLint("MissingInflatedId")
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_calculadora)

        /*val etNumero1: EditText = findViewById <EditText> (R.id.etNumero1)

        // Seta o evento de listener do click do objeto
        etNumero1.setOnClickListener {
            // Busca os objetos da tela e atribui em suas respectivas variáveis
            val etNumero1 = findViewById<EditText>(R.id.etNumero1)
            val etSenha = findViewById<EditText>(R.id.etSenha)
        }
        */


    }
}